{
  "name": "in-memory-rate-limiter",
  "version": "1.0.0",
  "description": "In-memory rate limiter with sliding window and token bucket strategies",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "build:ui": "vite build"
  },
  "keywords": [
    "rate-limiter",
    "sliding-window",
    "token-bucket",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.9",
    "typescript": "^5.5.2",
    "vite": "^5.4.2",
    "vitest": "^1.6.0"
  }
}
